{
  "name": "weser-wind-standalone",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/prepare-dev.mjs",
    "dev": "vite",
    "build": "node scripts/prepare-dev.mjs && vite build && node scripts/clean-dist.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vite": "^7.1.5"
  }
}
